{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Scrolltastic Story Language extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/extensions/vscode"],
      "preLaunchTask": "npm: build:vscode"
    }
  ]
}
