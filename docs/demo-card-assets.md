# Trading-card demo assets

The third demo package is reserved at:

`public/stories/c6c6bfa1-145e-4d68-a2fd-cc94107b46ea/`

The three card-front images now live in `assets/`; `story.json` references
them as standard Card Frames with OUT + CROP. The renderer preserves each
full front, uses the supplied dimensions to reserve layout, and expands the
declared normalized artwork window into the Panel as the reader scrolls.
